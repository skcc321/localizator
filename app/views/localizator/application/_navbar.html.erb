<nav class="navbar navbar-default">
  <div class="container">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href=<%= locales_url %>>Localizator</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <form action=<%= locales_url %> method="get" class="navbar-form navbar-left">
        <div class="form-group">
          <label for="filters_key">Key:</label>
          <input id="filters_key" name="filters[key]" type="text" value="<%= @filters["key"] %>" class="form-control" />
        </div>


        <div class="form-group">
          <label for="filters_text">Text:</label>
          <input id="filters_text" name="filters[text]" type="text" value="<%= @filters["text"] %>" class="form-control" />
        </div>

        <div class="form-group">
          <input id="filters_incomplete" name="filters[incomplete]" type="checkbox" <%= "checked=checked" if @filters["incomplete"] == "on" %> class="form-control" />
          <label for="filters_incomplete">Incomplete</label>
        </div>

        <div class="form-group">
          <input id="filters_empty" name="filters[empty]" type="checkbox" <%= "checked=checked" if @filters["empty"] == "on" %> class="form-control" />
          <label for="filters_empty">Empty</label>
        </div>

        <button type="submit" class="btn btn-default">Search</button>
      </form>
      <div class="navbar-right">
        <a href='/' class='btn btn-default navbar-btn'>App</a>
        <a href=<%= reload_locales_path %> class='btn btn-default navbar-btn'>Reload</a>
        <a href=<%= download_locales_path %> class='btn btn-default navbar-btn'>Download</a>
      </div>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
